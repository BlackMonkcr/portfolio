<nav
  class="fixed top-0 left-0 right-0 bg-[#18191d]/80 backdrop-blur-sm border-b border-gray-800 z-50"
>
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center h-16">
      <a href="/" class="text-white text-xl font-bold">AN</a>

      <!-- Menú hamburguesa para móvil -->
      <button id="menu-toggle" class="md:hidden text-gray-300 hover:text-white">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="h-6 w-6"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M4 6h16M4 12h16m-7 6h7"></path>
        </svg>
      </button>

      <!-- Menú desktop -->
      <ul class="hidden md:flex md:space-x-8">
        <li>
          <a
            href="#experiencia"
            class="text-sm text-gray-300 hover:text-white transition-colors"
            >Experiencia</a
          >
        </li>
        <li>
          <a
            href="#proyectos"
            class="text-sm text-gray-300 hover:text-white transition-colors"
            >Proyectos</a
          >
        </li>
        <li>
          <a
            href="#educacion"
            class="text-sm text-gray-300 hover:text-white transition-colors"
            >Educación</a
          >
        </li>
        <li>
          <a
            href="#sobre-mi"
            class="text-sm text-gray-300 hover:text-white transition-colors"
            >Sobre mí</a
          >
        </li>
        <li>
          <a
            href="#contacto"
            class="text-sm text-gray-300 hover:text-white transition-colors"
            >Contacto</a
          >
        </li>
      </ul>
    </div>

    <!-- Menú móvil -->
    <div id="mobile-menu" class="hidden md:hidden">
      <div class="px-2 pt-2 pb-3 space-y-1">
        <a
          href="#experiencia"
          class="block px-3 py-2 text-base text-gray-300 hover:text-white transition-colors"
          >Experiencia</a
        >
        <a
          href="#proyectos"
          class="block px-3 py-2 text-base text-gray-300 hover:text-white transition-colors"
          >Proyectos</a
        >
        <a
          href="#educacion"
          class="block px-3 py-2 text-base text-gray-300 hover:text-white transition-colors"
          >Educación</a
        >
        <a
          href="#sobre-mi"
          class="block px-3 py-2 text-base text-gray-300 hover:text-white transition-colors"
          >Sobre mí</a
        >
        <a
          href="#contacto"
          class="block px-3 py-2 text-base text-gray-300 hover:text-white transition-colors"
          >Contacto</a
        >
      </div>
    </div>
  </div>
</nav>

<script>
  const menuToggle = document.getElementById("menu-toggle");
  const mobileMenu = document.getElementById("mobile-menu");

  menuToggle?.addEventListener("click", () => {
    mobileMenu?.classList.toggle("hidden");
  });

  // Cerrar menú al hacer click en un enlace
  const mobileLinks = mobileMenu?.getElementsByTagName("a");
  if (mobileLinks) {
    Array.from(mobileLinks).forEach((link) => {
      link.addEventListener("click", () => {
        mobileMenu?.classList.add("hidden");
      });
    });
  }
</script>
